{{- with .Values.productService }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .label }}-service
  namespace: {{ $.Values.global.namespace }}
spec:
  type: ClusterIP
  selector:
    app: {{ .label }}
  ports:
    - protocol: TCP
      port: {{ .servicePort }}
      targetPort: {{ .containerPort }}
{{- end }}